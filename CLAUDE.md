# AGENTS

## 工作流编排（Workflow Orchestration）

### 1. 默认进入 Plan 模式（Plan Node Default）

- 在进入完整 Plan 流程前，先进行复杂度评估。简单任务可采用轻量计划；非简单任务（3 个以上步骤、涉及架构决策、跨模块修改或高验证成本）必须进入完整 Plan 流程。
- 简单任务通常指单文件、小范围、无架构影响、无接口变化、易于直接验证的修改，例如文案修改、局部逻辑修复、小型重命名。
- 如果事情开始偏离预期，立刻停止并重新规划，不要硬着头皮继续做。
- Plan 模式不仅用于构建阶段，也用于验证步骤。
- 提前写出详细的规格说明（spec），减少歧义。

### 2. 子代理策略（Subagent Strategy）

- 在能显著提升并行效率、降低主上下文负担或改善分析质量时使用 Subagent；避免为简单、阻塞主路径或边界不清的任务机械使用 Subagent。
- 将研究、探索、并行分析等任务交给 Subagent。
- 对于复杂问题，可以通过 Subagent 投入更多计算资源。
- 每个 Subagent 只负责一个任务，保证执行专注。

### 3. 自我改进循环（Self-Improvement Loop）

- 只要用户做出任何纠正，就把经验模式写入 `tasks/lessons.md`。
- 为自己写规则，防止同样的错误再次发生。
- 持续、严格地迭代这些经验，直到错误率下降。
- 在每次会话开始时，先查看 `tasks/lessons.md` 是否与当前项目相关。

### 4. 完成前必须验证（Verification Before Done）

- 没有证明功能有效之前，不要标记任务完成。
- 在相关情况下，对比主分支与修改后的行为差异（diff）。
- 在完成前自问一句：“一位资深工程师会批准这个实现吗？”
- 运行测试、检查日志、证明功能正确。

### 5. 追求优雅（Demand Elegance，平衡）

- 对于非简单修改，先停下来问：“有没有更优雅的实现方式？”
- 如果一个修复方案显得很 hacky，就在当前已知信息下实现更优雅的方案。
- 对于简单明显的修复，可以跳过这一点，避免过度设计。
- 在提交之前，先挑战自己的实现。
- 对于“优雅”或“资深工程师会批准吗”的判断，必须同时检查客观标准：是否符合单一职责、是否引入不必要依赖、是否扩大模块耦合、是否增加不必要抽象、是否明显提高实现复杂度、是否使验证更困难。
- 主观判断只能作为补充提醒，不能替代客观检查。

### 6. 自动化修 Bug（Autonomous Bug Fixing）

- 当收到 Bug 报告时，直接修复，不要等待手把手指导。
- 查看日志、错误信息、失败的测试，然后解决问题。
- 不要要求用户进行上下文切换。
- 发现 CI 测试失败时，主动修复，而不是等待指示。

### 7. Blast Radius（影响范围评估）

- 在自动修复 Bug 前，必须评估修改的 Blast Radius。
- 若修改涉及数据库 Schema 或数据迁移、认证/授权/计费/安全边界、部署配置/环境变量/CI/基础设施、公共接口、核心抽象、并发模型、删除逻辑，或无法通过现有测试与日志快速验证正确性，则视为高风险，必须暂停并请求人工确认。
- 当修改跨多个模块或涉及超过 3 个文件时，应视为潜在高风险，并结合 Blast Radius 进一步判断是否需要人工确认。

## 任务管理（Task Management）

1. **Plan First**  
   在进入完整 Plan 流程前先评估复杂度。非简单任务将计划写入 `tasks/todo.md`，并拆分成可勾选的任务；简单任务可采用轻量计划，但仍需说明目标、修改范围与验证方式。
2. **Verify Plan**  
   在开始实现之前进行确认。
3. **Track Progress**  
   执行过程中持续标记完成的任务。
4. **Explain Changes**  
   每一步都提供高层级变更说明。
5. **Document Results**  
   在 `tasks/todo.md` 中添加 review / 复盘部分。
6. **Capture Lessons**  
   修正问题后更新 `tasks/lessons.md`。

## 核心原则（Core Principles）

- **Simplicity First（简单优先）**  
  每一次改动都尽可能简单，只影响最少代码。
- **No Laziness（不偷懒）**  
  找到根本原因，不做临时修补，按照资深工程师标准实现。
- **Minimal Impact（最小影响）**  
  只修改必要部分，避免引入新的 Bug。

## Code Design（代码设计）

- 先设计稳定边界，再填充实现细节；宁可多花一点时间澄清职责，也不要靠后期补丁维持结构。
- 优先通过组合、小接口和新增实现来扩展系统，避免为局部需求修改稳定核心或引入过度抽象。
- 使用中文注释

## Safety Rules（安全规则）

- 不回滚非本人修改。
- 不做未获批准的破坏性命令。
- 遇到高风险歧义先停下确认。

## gstack（推荐）

本项目推荐使用 [gstack](https://github.com/garrytan/gstack) 辅助 AI 工作流。
如需获得完整体验，请先安装：

```bash
git clone --depth 1 https://github.com/garrytan/gstack.git ~/.claude/skills/gstack
cd ~/.claude/skills/gstack && ./setup --team
```

安装后可使用 `/qa`、`/ship`、`/review`、`/investigate`、`/browse` 等技能。
进行网页浏览时优先使用 `/browse`。
引用 gstack 文件路径时使用 `~/.claude/skills/gstack/...`。
