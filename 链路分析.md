后端链路

- 启动装配链：/G:/gofile/Agent_Project/apps/server/cmd/server/main.go
  :1 -> config.Load -> logging.NewLogger ->
  postgres.NewPool/RunMigrations -> ResourceRepo/TaskRepo/ApprovalRep
  o/JobRepo/TaskEventRepo/AssistantRepo/UploadedFileRepo -> embedder/
  reranker/retriever/ingest -> planner/reviewer/editor/executor -> ta
  sk events service -> job.Worker -> handlers -> /G:/gofile/Agent_Pro
  ject/apps/server/internal/server/router/router.go:1 -> Hertz Run。
- 配置与可观测性链：internal/config/config.go 负责 .env + YAML + 环境
  变量 合并；observability/logging/logger.go 负责 slog；observability/
  logging/context.go 负责 request_id/task_id/run_id/step_name 上下文；
  server/middleware/request_context.go 和 cors.go 负责请求日志、X-
  Request-ID、CORS。
- 基础 HTTP 链：router.Register -> handlers/health.go -> /healthz；
  handlers/preflight.go -> OPTIONS /api/*path。
- 资源读取链：handlers/resources.go ->
  ResourceRepo.List/GetByID/GetCurrentVersion -> 返回 resources 与 re
  source_versions。
- 资源检索链：handlers/resources.go:Search -> knowledge/retriever/
  service.go -> embedder.Embed 生成 query vector ->
  ResourceRepo.SearchChunksByResource + SearchChunksLexicalByResource
  双路召回 -> knowledge/reranker/reranker.go 重排 -> knowledge/
  citation/builder.go 产出 citations。
- 文档导入链：assistant/importer.go 或启动时 ingest.ImportDirectory ->
  knowledge/ingest/service.go -> knowledge/chunker/chunker.go 分块 ->
  knowledge/embedder/embedder.go 向量化 ->
  ResourceRepo.Create/CreateVersion/CreateChunk 落 resources/resource
  _versions/resource_chunks。
- 助手会话链：handlers/assistant.go -> assistant/service.go ->

AssistantRepo.CreateSessionWithMessages/AppendMessages/ListMessages； 流式回复分支继续走
assistant/chat_responder.go，由 replyJSONStreamExtractor 把模型 JSON
流拆成 SSE message_delta/message_completed/task_suggestion。
- 助手上传链：AssistantHandler.UploadFile ->
  assistant.Service.UploadFile -> storage/filestore/local.go 保存原文
  件 -> storage/postgres/uploaded_files.go 记元数据 ->
  assistant.IngestDocumentImporter -> ingest.Service.ImportDocument ->
  会话里追加 session_file 消息。
- 文件下载链：handlers/files.go -> UploadedFileRepo.GetByID ->
  LocalStore.Open -> 附件下载响应。
- 助手转任务链：assistant.Service.ConfirmTaskSuggestion -> task/
  service/service.go:CreateTask -> TaskRepo.Create -> 异步起 task/
  workflow/orchestrator.go。
- 任务编排链：Orchestrator -> task/models/task.go 校验状态流转 -> pla
  nner/agent.go 产出 intent/search_queries/focus_sections -> retrieve
  r.Service.SearchByResource -> reviewer/agent.go 产出 review_summary
  -> editor/agent.go 产出 diff_preview -> TaskRepo.AddStep/AddArtifact
  -> ApprovalRepo.Create -> awaiting_approval。
- 任务事件链：task/events/service.go 统一校验事件输入 -> storage/
  postgres/task_events.go 落库；调用方包括 task.Service、
  Orchestrator、approval.Service、job.Worker。
- 任务查询链：handlers/tasks.go -> task.Service.GetTask/ListTasks + T
  askRepo.GetArtifacts + TaskEventRepo.ListByTask -> 对外提供 /api/ta
  sks、详情、产物、事件流。
- 审批链：handlers/approvals.go -> approval/service.go ->
  ApprovalRepo.List/GetByID/UpdateStatus；批准时继续 JobRepo.Create +
  TaskRepo.UpdateStatus(executing)，拒绝时直接 TaskRepo.UpdateStatus(
  failed)。
- 执行链：approval.Service.Approve -> job.Worker 的 jobCh ->
  JobRepo.ClaimNext -> agent/executor/agent.go 读取 diff_preview、当前
  版本并按章节替换 -> ResourceRepo.CreateVersion 生成新版本 ->
  JobRepo.UpdateStatus(done/failed) ->
  TaskRepo.UpdateStatus(completed/failed)。
- 数据层模块总表：storage/postgres/db.go 管连接池与迁移；resources.go
  管资源/版本/分块；tasks.go 管任务/步骤/产物；approvals.go 管审批与
  execution job；assistant.go 管会话与消息；task_events.go 管事件；
  uploaded_files.go 管原文件元数据；migrations/*.sql 管表结构。

前端与运行链路

- 前端总装配链：/G:/gofile/Agent_Project/apps/web/app/layout.tsx:1
  -> /G:/gofile/Agent_Project/apps/web/components/app-chrome.tsx:1
  -> /G:/gofile/Agent_Project/apps/web/components/nav.tsx:1 -> App
  Router 各页面；lib/api/client.ts 是所有请求的统一入口，lib/
  terminal.ts 是状态/时间/错误格式化入口。
- 助手页链：/G:/gofile/Agent_Project/apps/web/app/page.tsx:1 ->
  components/assistant/assistant-shell.tsx -> assistant-composer.tsx、
  assistant-message-list.tsx、session-history.tsx -> lib/api/
  assistant.ts + lib/api/assistant-stream.ts -> 后端 assistant API；消
  息结构统一在 lib/assistant/types.ts。
- 资源页链：app/resources/page.tsx -> components/resource-list.tsx ->
  lib/api/resources.ts -> /api/resources。
- 任务创建页链：app/tasks/new/page.tsx -> task-create-client.tsx ->
  components/task-create-form.tsx + components/resource-search.tsx ->
  lib/api/resources.ts + lib/api/tasks.ts -> /api/resources/:id、/api/
  resources/:id/search、/api/tasks。
- 任务详情页链：/G:/gofile/Agent_Project/apps/web/app/tasks/[id]/
  page.tsx:1 -> lib/api/tasks.ts 并行拉 getTask/getTaskArtifacts/
  getTaskEvents -> components/task-timeline.tsx、citation-list.tsx、
  diff-preview.tsx -> UI 原语 ui/terminal-frame.tsx、status-chip.tsx、
  meta-row.tsx、log-line.tsx、diff-pane.tsx。
- 审批页链：app/approvals/page.tsx -> lib/api/approvals.ts -> /api/
  approvals、/api/approvals/:id/approve、/api/approvals/:id/reject；详
  情型 API getApproval/getJob 已有 client，但当前页面主用列表和审批动
  作。
- 前端构建链：apps/web/package.json ->
  next.config.mjs(output=standalone) -> apps/web/Dockerfile 生成
  standalone 运行镜像。
- 本地联调链：/G:/gofile/Agent_Project/scripts/dev/start-local.ps1:1
  -> 启 Go server(go run ./apps/server/cmd/server) + Next dev；status-
  local.ps1 查状态；stop-local.ps1 按状态文件停进程。
- 本地基础设施链：根 docker-compose.yml 只起本地 PostgreSQL/pgvector。
- 生产部署链：.github/workflows/ci.yml 跑 go test、npm run build、
  Docker build；.github/workflows/release-deploy.yml 构建并推 GHCR 镜
  像、通过 SSH 传镜像到远端、结合 deploy/docker-compose.prod.yml 起
  web/server；scripts/deploy/remote-deploy.sh 是远端部署脚本。
- 运行配置链：根 go.mod 定义 Go 服务依赖；config/default.yaml 提供
  server/AI/log 默认值；apps/server/Dockerfile 构后端镜像。